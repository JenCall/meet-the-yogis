<br>
<h2 style="display:flex; flex-direction: row; justify-content: center; align-items: center;" > Teachers in Stockholm</h2>
<!-- Insert the location afterwards-->

<hr>
<div class="search-bar" style="display:flex; flex-direction: row; justify-content: center; align-items: center;">
  <p>&#10004; Location</p>
  <p>&#10004; Date</p>
  <p>&#10004; Style</p>
</div>

<hr>

<div class="form-inputs">
  <%= simple_form_for :query, url: teachers_path, method: "GET" do |f| %>
    <%= f.input :address, collection: @addresses, label: false, prompt: "City"%>
    <%= f.input :date, as: :date, start_year: Date.today.year,
                                end_year: Date.today.year+1,
                                order: [:day, :month, :year], include_blank: true %>
    <%= f.input :style, collection: @styles, label: false, prompt: "Style"%>
    <%= f.input :price, collection: @prices, label: false, prompt: "Price"%>
    <%= f.input :level, collection: @levels, label: false, prompt: "Level"%>
    <%= f.input :ratings, collection: @ratings, label: false, prompt: "Rating"%>
    <%= f.button :submit, "Apply filter", class: "search-btn" %>
  <% end %>
</div>


<!--
Location - Enter text, marke when applied
Date - Open calendar - fetch data from the course object.
Style - Open dropdown list populated from the teacher style alternative.
Price - Open search bar populated from the course price.
Level - Open dropdown list populated from the course level list.
Ratings - Open dropdown list populated from the course list.


-->


<div class="search-bar" style="display: flex; flex-direction: row; justify-content: center; align-items: center;">
  <p>Price</p>
  <p>Level</p>
  <p>Ratings</p>
</div>

<hr>

<div class="d-flex flex-column justify-items-center align-items-center">

  <% search_items = ["yoga", "pilates", "yogis", "yogi", "yoga teacher", "yoga trainer", "yoga class"] %>

  <% @teachers.each do |teacher| %>
    <div class="card" style="width: 18rem;" >
      <img src="https://source.unsplash.com/featured/?<%= search_items.sample %>" class="card-img-top" alt="" style="height: 10rem; object-fit: cover">

      <div class="card-body">
        <h5 class="card-title"><strong><%= teacher.first_name %></strong> <strong><%= teacher.last_name %></strong> <i class="far fa-heart"></i></h5>
        <% if teacher.certification == true %>
        <p> Certified Teacher | <%= teacher.certificationhours %>h Yoga Alliance </p>
        <% else %>
        <p> Community Teacher </p>
        <% end %>
        <hr>
        <p class="card-text"> <%= teacher.style %> </p>
        <p class="card-text"> <%= "from #{[15.99, 18.50,12.99,13.99,14.75].sample}" %><i class="fas fa-euro-sign"></i> / day</p>
        <div class="card-footer">
          <div class="text-muted">
            <% teacher.ratings.times do %>
              <i class="fas fa-heart"></i>
            <% end %>
            </div>
        </div>
        <button type="button" class="btn btn-outline-success" ><%= link_to "See more", teacher_path(teacher) %></button>
        <hr>
      </div>
    </div>
    <hr>
  <% end %>
</div>

<div class="jumbotron">
  <p>Create your account today, it is easy and quick!</p>
  <a class="btn btn-outline-success" href="#" role="button">Create an account</a>
  <hr>
  <br>
  <div>
    <%= link_to "Home", root_path, class:"btn btn-outline-success" %>
  </div>
